import React from 'react';
import Typography from '@mui/material/Typography';
import Box from '@mui/material/Box';

function XRay() {
  return (
    <Box p={3}>
      <Typography variant="h4">X-Rays</Typography>
      <Typography variant="body1">This is where X-Ray images will be displayed.</Typography>
    </Box>
  );
}

export default XRay;